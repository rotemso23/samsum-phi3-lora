# Core ML
datasets==4.8.0
transformers>=4.40.0
peft==0.11.1
bitsandbytes==0.43.1
accelerate==0.30.1

# Evaluation (training/eval only — not needed by the Space)
rouge-score==0.1.2

# Experiment tracking (training only — not needed by the Space)
# mlflow==2.13.0  ← conflicts with datasets>=4.x (pyarrow version clash)
# Install manually in Colab/Kaggle: pip install mlflow==2.13.0

# Demo UI
gradio>=5.0.0
pyaudioop  # pydub compatibility shim for Python 3.13 (audioop was removed)

# Utilities
python-dotenv==1.0.1
